apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "networking-toolbox.fullname" . }}-test-connection"
  labels:
    {{- include "networking-toolbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "networking-toolbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
