apiVersion: v1
kind: Service
metadata:
  name: {{ include "networking-toolbox.fullname" . }}
  labels:
    {{- include "networking-toolbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "networking-toolbox.selectorLabels" . | nindent 4 }}
