apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-terminal.fullname" . }}
  labels:
    {{- include "open-terminal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.openTerminal.server.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "open-terminal.selectorLabels" . | nindent 4 }}
