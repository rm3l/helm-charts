apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-terminal.fullname" . }}-test-connection"
  labels:
    {{- include "open-terminal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open-terminal.fullname" . }}:{{ .Values.service.port }}/docs']
  restartPolicy: Never
