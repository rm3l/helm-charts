# Existing secret test
# Tests using an existing secret for API key

openTerminal:
  apiKey:
    # Note: In real CI, you'd create this secret beforehand
    existingSecret: "open-terminal-api-key"
    existingSecretKey: "api-key"

persistence:
  enabled: true
  existingClaim: "open-terminal-data"
